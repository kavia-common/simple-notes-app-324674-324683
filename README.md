# Simple Notes (React, frontend-only)

This repository contains a simple, frontend-only notes application built with React. Users can add, edit, delete, and search notes. Notes are stored locally in the browser using `localStorage`, so no backend or database is required.

## Project layout

The app lives in the `notes_frontend/` directory:

- `notes_frontend/` – React application (Create React App)

## Quick start

1. Change into the frontend directory:

   ```bash
   cd notes_frontend
   ```

2. Install dependencies:

   ```bash
   npm install
   ```

3. Run the app:

   ```bash
   npm start
   ```

Then open http://localhost:3000.

## Documentation

For detailed setup, usage, and implementation notes, see:

- `notes_frontend/README.md`
- `kavia-docs/CodeWiki/Onboarding/simple-notes-app.md`
- `kavia-docs/CodeWiki/Specs/ArchitectureSpecs/notes-frontend-architecture.md`
- `kavia-docs/CodeWiki/Specs/Other/theme-and-ui-notes.md`
